# Secrets / clés API
test_voix/cles_api.txt
data/config.json
# Quel moteur de voix lourd relancer au démarrage (START.bat) : reglage
# propre a CHAQUE machine, ecrit tout seul par DEMARRER_XTTS.bat ou
# DEMARRER_KYUTAI.bat -- il ne doit pas voyager dans le depot.
data/moteur_voix.txt
# Notes prises en ecoutant les voix dans le lecteur (fenetre « Ecouter les
# voix ») : observations personnelles de Laurent sur SA machine.
data/annotations_voix.json

# Python
__pycache__/
*.pyc

# Base de données / logs locaux
data/nimm_epub.db
*.db-journal
*.log
data/tts_cache/

# Résultats de test (contiennent des extraits du livre, pas à versionner)
test_voix/resultat_*.txt
test_voix/chapitre_test.txt

# Modèles Kokoro (trop volumineux pour Git, ~350 Mo à eux deux --
# à copier manuellement d'une machine à l'autre si besoin)
kokoro-v1.0.onnx
voices-v1.0.bin

# Modeles Piper (60-75 Mo chacun -- idem, copie manuelle entre machines)
fr_FR-*.onnx
fr_FR-*.onnx.json

# Livres et couvertures : JAMAIS dans Git. Fichiers volumineux, et certains
# sont sous droits d'auteur : les EPUB de Laurent ne doivent pas sortir du PC.
data/library/

# Fichiers de travail et residus de test : extraits de livres, audio de test,
# binaires, sauvegardes locales. Rien de tout cela n'a sa place sur le depot.
data/_tts_req.json
data/journal_tokens.csv
data/journal_erreurs_gemini.log
data/journal_erreurs_casting.log
data/rapport_modeles_locaux.txt
data/rapport_modeles_locaux*.txt
data/point_*.txt
# Fichiers de travail et sorties de mes analyses/outils (toujours prefixes
# d'un souligne) : jamais a versionner.
data/_*.txt
test_voix/*.json
test_voix/*.wav
test_voix/test_pid.txt
test_voix_nimm.wav
server_pid.txt
*.bak_*

# Journaux des longs traitements lances en tache de fond (16/09/2026) et
# captures d'ecran personnelles de Laurent : residus de session locaux.
test_voix/*.out.txt
test_voix/*.err.txt
Screenshot_*.jpg

# Lots d'ecoute regeneres par les outils de test_voix (16/09/2026) : les WAV
# sont deja ignores plus haut, mais leurs index et leurs listes portent des
# titres de livres et des observations personnelles -- on les garde sur le PC.
test_voix/ecoute_*/
test_voix/point_*/
test_voix/point_large_*/
test_voix/aveugle_*/
test_voix/stabilite_*/

# Moteur Kyutai TTS (environnement Python 3.12 + voix + poids, non
# versionnes : voir kyutai_service/LIRE_MOI.md pour les recreer)
kyutai_service/.venv/
kyutai_service/voix_fr/
kyutai_service/modele/
kyutai_service/sortie_ecoute/
kyutai_service/sortie_ecoute_toutes/
kyutai_service/sortie_ecoute_etrangeres/
kyutai_service/sortie_ecoute_cc0/
kyutai_service/voix_autres/
kyutai_service/_tmp_wheel/
kyutai_service/journal_installation.txt
kyutai_service/journal_ecoute_cc0*.txt

# Extraits de voix du DOMAINE PUBLIC : travail personnel de Laurent (15/09/2026)
# -- enregistrements sources, versions nettoyees dans Audacity, brouillons de
# montage. Ces fichiers restent sur ce PC : jamais sur le depot, ni dans son
# historique (meme regle que les livres). Les voix REELLEMENT utilisees par les
# moteurs vivent dans xtts_service/voix_fr/ et kyutai_service/voix_fr/, deja
# ignores ci-dessus.
Extraits de voix/
extraits_voix/
dossier_voix/
# Tout format audio d'extrait : Audacity exporte en WAV (ou AIFF), les sources
# sont souvent en MP3, FLAC ou OGG. Aucun audio n'a sa place dans le depot.
*.mp3
*.flac
*.ogg
*.opus
*.m4a
*.aif
*.aiff
*.wav

# Moteur XTTS v2 (environnement Python 3.12 + extraits de voix + poids, non
# versionnes : voir xtts_service/LIRE_MOI.md pour les recreer)
xtts_service/.venv/
xtts_service/voix_fr/
xtts_service/_tmp_wheel/
xtts_service/sortie_ecoute/
xtts_service/sortie_ecoute_cml/
xtts_service/sortie_ecoute_guillemets/
xtts_service/journal_installation.txt
xtts_service/journal_cml*.txt

# Lots d'ecoute des voix XTTS issues d'extraits libres de droits (outil
# _ecouter_extraits_dp.py) : audio produit par le moteur, jamais versionne.
xtts_service/sortie_ecoute_dp/

# Livres audio exportes en MP3 (idee de Laurent, 15/09/2026 : exporter un livre
# avec son casting pour l'ecouter comme un vrai livre audio). Ce sont des
# OEUVRES et de l'audio produit par les moteurs de voix : ils ne doivent jamais
# entrer dans le depot, ni dans son historique.
data/exports/

# Ancien historique Git, mis de cote le 15/09/2026 quand le depot a ete reparti
# de zero (l'ancien historique contient des livres) : jamais versionne.
.git_ancien_*/

